import PhoneSelector from '../components/PhoneSelector/index.tsx';

export default {
  title: 'Slider',
  component: PhoneSelector,
};

const Template = () => <PhoneSelector />;

export const Default = Template.bind({});
